local ID = require('stdlib.objediting.stdlibids')

local abil = AbilityDefinitionDefend:new(ID.Abilities.UnitEnterLeaveDectect)
abil:setName('Leave Dectect')
abil:setEditorSuffix('(UnitTracker)')
abil:setArtCaster('')
abil:setIconNormal('')
abil:setRace(Race.Unknown)
